public class Lifebar extends AnimatedThing{

    // Constructeur
    public Lifebar(double x, double y, int attitude, int index, int maxIndex, double l, double h, String fileName) {
        super(x, y, attitude, index, maxIndex, l, h, fileName);
    }



}
